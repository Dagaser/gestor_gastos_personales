# DEPENDENCIAS MÍNIMAS PARA EL ESTADO ACTUAL DEL PROYECTO
# (Solo lo que realmente se está usando)

# Framework principal
Django>=4.2,<5.0

# Para manejo de imágenes (FileField de comprobantes)
Pillow>=8.0.0

# Para export PDF (ya lo tenías funcionando)
xhtml2pdf>=0.2.5

# Para export Excel (nueva funcionalidad)
openpyxl>=3.0.0

# Para variables de entorno (si estás usando)
python-decouple>=3.6

# NUEVAS DEPENDENCIAS que agregamos en las mejoras:
# (Estas son opcionales, el proyecto funciona sin ellas)

# Para validación de tipos MIME reales (opcional)
# python-magic>=0.4.27

# Para logging estructurado (opcional) 
# python-json-logger>=2.0.7